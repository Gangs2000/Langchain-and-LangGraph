[project]
name = "deepagents"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Gangs2000", email = "gangeswaransmart@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "black>=26.5.1",
    "dotenv>=0.9.9",
    "isort>=9.0.1",
    "langchain-ollama>=1.1.0",
    "langgraph>=1.2.11",
]

[project.scripts]
deepagents = "deepagents:main"

[build-system]
requires = ["uv_build>=0.12.12,<0.13.0"]
build-backend = "uv_build"
